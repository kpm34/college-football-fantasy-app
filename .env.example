# ===== Environment Variables (example) =====
# Copy this file to `.env.local` for local development.
# In production on Vercel, set the same variables in Project Settings → Environment Variables.

# --- Vercel Project Identification ---
# Used when interacting with the Vercel REST API (e.g., listing deployments, creating env vars via API,
# scripting CI automations). Not required for normal Next.js runtime.
# Safe to share; it is NOT a secret. You will still need a `VERCEL_TOKEN` for authenticated API calls.
VERCEL_PROJECT_ID=prj_ymD2jeWiWhbTrxDvLfKV4gdgd8qD

# Optional: only if you need to read the Project ID in the browser (client-side).
# Avoid exposing secrets in NEXT_PUBLIC_* variables; this specific ID is fine to expose.
NEXT_PUBLIC_VERCEL_PROJECT_ID=prj_ymD2jeWiWhbTrxDvLfKV4gdgd8qD

# If you plan to call the Vercel API from server-side scripts, you will also need:
# VERCEL_TOKEN=your_vercel_api_token
# VERCEL_ORG_ID=team_or_org_id

# Usage in server code (Node/Next.js):
#   const projectId = process.env.VERCEL_PROJECT_ID;
# Usage in client code (browser):
#   const projectId = process.env.NEXT_PUBLIC_VERCEL_PROJECT_ID;

# Appwrite Configuration
APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
APPWRITE_PROJECT_ID=your-project-id
APPWRITE_API_KEY=your-api-key
APPWRITE_DATABASE_ID=default

# Frontend Public Variables (exposed to browser)
NEXT_PUBLIC_APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
NEXT_PUBLIC_APPWRITE_PROJECT_ID=your-project-id

# College Football Data API
CFBD_API_KEY=your-cfbd-api-key

# Redis (Upstash)
REDIS_URL=redis://default:your-password@your-endpoint.upstash.io:port

# Optional: Season Year (defaults to current year)
SEASON_YEAR=2024

# AI Gateway Configuration
AI_GATEWAY_API_KEY="your-ai-gateway-api-key"

# Worker-specific (not needed for Vercel)
# APPWRITE_FUNCTIONS_ENDPOINT=
# APPWRITE_FUNCTIONS_KEY=
# FANTASY_SCORING_JSON=